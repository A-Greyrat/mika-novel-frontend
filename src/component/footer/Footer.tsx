import './Footer.less';
import {memo} from "react";

const Footer = () => {
    return (
        <footer className="mika-novel-footer-container">
            <p>Footer</p>
        </footer>
    )
}

export default memo(Footer);